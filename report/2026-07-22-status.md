# Niko-Agent 阶段状态（2026-07-22）

## 工作位置

- 仓库：`D:\codex_project\Niko-Agent`
- 集成分支：`feature/niko-v1`
- 当前隔离分支：`work/niko-v1-recovery-eval`
- 远程：`git@github.com:usercomeing/Niko-Agent.git`

## 已完成

- 从 pico 基线导入并完成 Niko 品牌、包名、CLI、配置与状态目录调整。
- 增加敏感路径保护、会话 fork、checkpoint 快照、回滚预览和一次性确认 token。
- 增加 quick/full verify 和 deterministic evaluate 入口。
- 快速离线门禁：Ruff 通过，核心测试 21/21 通过。
- Windows verifier 的 `python3` 兼容修复后，确定性评测由 0/12 提升到 10/12。
- 已终止两组超时后遗留的 `uv -> pytest -> python` 进程树。
- 已将项目基线按文档、产品代码、验证套件三个阶段提交到隔离分支。
- `workspace_mismatch_resume` 已改为 fail-closed：不调用模型、不执行工具、不猜测缺失状态，并记录审计事件。
- benchmark 注入 checkpoint 后会重新计算 resume 状态，覆盖 freshness 和 workspace mismatch 场景。

## 当前判断

- `workspace_mismatch_resume` 的评测契约现验证 `unsafe_resume_state`、零工具步骤、无 `model_requested`、README 未改和 mismatch 审计事件。
- `freshness_reanchor_resume` 已修复评测 setup 的状态刷新，但尚未执行评测确认。
- 上游异步 worker 验收在 Windows 上存在长时间等待，需要继续定位线程退出或 scripted client 时序。

## 本阶段提交

- `9929617 docs: establish Niko-Agent project foundation`
- `a79445a feat: add Niko-Agent runtime and recovery core`
- `2fe46ec test: add offline verification and evaluation suites`
- `cb9e759 fix(recovery): fail closed on workspace mismatch`

## 验证说明

按用户明确要求，本阶段在终止遗留进程后跳过测试。上述修改仅完成 diff/范围检查；不得将其表述为测试通过。此前的 21/21 quick 和 10/12 deterministic 结果发生在本阶段修改之前。

## 下一阶段

1. 创建当前隔离分支到 `feature/niko-v1` 的 PR，保留“未运行测试”标记。
2. 后续如恢复测试，优先确认 workspace mismatch 与 freshness 两个确定性场景。
3. 定位 Windows worker 长等待并补充线程退出保护。
4. PR 审查通过后合并回 `feature/niko-v1`。
